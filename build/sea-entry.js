// Entry point for the SEA (Single Executable Application) build.
// This only starts the web UI server — the full CLI is for terminal users.
import { startServer } from '../src/server.js';
startServer();
